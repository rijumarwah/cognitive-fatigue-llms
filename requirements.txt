# Core library (fatigue/) — signal math only, no model-loading required.
numpy>=1.24
torch>=2.0

# Needed only to run experiments/rq2.py, experiments/helper.py, or
# fatigue.monitor.FatigueMonitor end-to-end against real HF models.
transformers>=4.40
datasets>=2.18
accelerate>=0.30
bitsandbytes>=0.43; platform_system == "Linux"
matplotlib>=3.7
pandas>=2.0
scipy>=1.10
seaborn>=0.13
scikit-learn>=1.3
pyyaml>=6.0
