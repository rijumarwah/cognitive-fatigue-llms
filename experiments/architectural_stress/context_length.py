#!/usr/bin/env python3
"""
Placeholder. Context-length and positional-sensitivity stress tests
(Figures 2-3) are implemented in ../rq2.py (--run_context / --run_positional),
which uses `fatigue/` for signal computation -- that is the current source
of truth, not this file. See the README.md in this directory.
"""

import sys

if __name__ == "__main__":
    sys.exit(
        "context_length.py is not implemented. Use "
        "`python experiments/rq2.py --run_context --run_positional` instead."
    )
